language: node_js
node_js:
  - 0.10
branches:
  only:
    - master
script: npm install && npm test && for i in test/unit/*; do ./node_modules/.bin/jaribu $i; done && ./node_modules/.bin/jaribu test/unit/baseclient/types-suite.js
