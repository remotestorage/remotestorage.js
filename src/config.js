var config = {
	logging: false,
	changeEvents: {
      local:    true,
      window:   false,
      remote:   true,
      conflict: true
  },
  cache: true,
  discoveryTimeout: 10000,
  cordovaRedirectUri: undefined
};

module.exports = config;